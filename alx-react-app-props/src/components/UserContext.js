import React from "react";

// Initialize context using React.createContext() as per instructions
const UserContext = React.createContext(null);
// Export the context as default export
export default UserContext;
