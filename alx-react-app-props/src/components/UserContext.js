import { createContext } from "react";

// Create the UserContext
const UserContext = createContext(null);

// Export the context as default export
export default UserContext;

// You can also provide a named export if needed
export { UserContext };
